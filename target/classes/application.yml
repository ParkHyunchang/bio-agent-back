spring:
  profiles:
    active: local
  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 100MB

server:
  port: 3211

logging:
  level:
    org.springframework.web: INFO

anthropic:
  api:
    key: ${ANTHROPIC_API_KEY:}
